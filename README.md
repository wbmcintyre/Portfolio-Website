# Portfolio Website

The objective of this project is to display Ben's experience/portfolio on a website. The project was primarily built with React and SASS modules. 

### How to run locally

Execute `npm install` to download all node module dependencies.

Afterwards, execute `npm start` to run the app in the development mode.
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.


## Future Updates
1. Replace SASS modules with styled components
2. Use SVGR to replace SVG components
3. Utilize email verification through backend instead of "mailto:"
4. Utilize Next JS in backend
5. Research Canvas optimizations
